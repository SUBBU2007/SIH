# MongoDB Atlas connection string (Database > Connect > Drivers)
MONGODB_URI=your_mongodb_atlas_connection_string

# Optional: only needed if you switch OCR/AI explanation calls server-side
ANTHROPIC_API_KEY=your_key_if_used
